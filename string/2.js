// 2. Write a JavaScript function to check whether a string is blank or not.

const blank = input => {
    if (input.length === 0){
        return true;
    } else {
        return false
    }
}

console.log(blank('sd'))